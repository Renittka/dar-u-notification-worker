# Application
server.port=8025
spring.application.name=notification-worker

# Eureka
eureka.client.service-url.defaultZone=http://localhost:8010/eureka

#eureka.instance.instance-id=${spring.application.name}:${spring.application.instance_id:${random.value}}
server.servlet.context-path=/
spring.cloud.discovery.enabled=true
eureka.client.fetch-registry=true
eureka.client.register-with-eureka=true
eureka.instance.prefer-ip-address=true

# Kafka config
spring.kafka.bootstrap-servers=localhost:9092

spring.kafka.topic=quickstart-events

spring.kafka.consumer.group-id=quickstart-events-0
spring.kafka.consumer.auto-offset-reset=latest

spring.mail.host=smtp.mail.ru
spring.mail.port=465
spring.mail.username=testkafka7@mail.ru
spring.mail.password=7B1HGxEdbbCFRsyfDzag
spring.mail.protocol=smtp

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.enable=true

